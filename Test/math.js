// import add from './demo.js';

// console.log(add(2,3));

const add = require('./demo.js');

console.log(add(2,3));

//difference in type module and type common js is that in type module we can use import and export and in type commonjs we can use require and module.exports